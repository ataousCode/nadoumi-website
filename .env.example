# Nadoumi Frontend — Vercel Production Environment Template

# The full URL to your Railway backend API (including /api)
VITE_API_BASE_URL=https://your-backend-railway-url.app/api

# Optional: Other configuration keys if added later
# VITE_GOOGLE_ANALYTICS_ID=...